# Расширенная система отзывов - Документация

## 📋 Описание

Реализована полноценная система отзывов по модели Uber/Яндекс GO с детальными критериями оценки, комплиментами и публичными комментариями.

## ✨ Возможности

### 1. Детальные критерии оценки (1-5 звезд)
- ⏰ **Пунктуальность** - приехал вовремя
- 📦 **Качество упаковки** - бережное обращение с грузом
- 👔 **Профессионализм** - общее впечатление от работы
- 💬 **Общение** - вежливость и отзывчивость
- 🚛 **Состояние транспорта** - чистота и исправность машины

### 2. Система комплиментов (значков)
Пользователи могут отметить водителя/заказчика значками:
- ⭐ Супер водитель
- ⚡ Быстрая доставка
- 📦 Бережная упаковка
- 👔 Профессионал
- 😊 Приветливый
- ✨ Чистая машина
- ⏰ Пунктуальный
- 💬 Хорошее общение
- 💰 Честная цена
- 🎯 Надёжный

### 3. Публичные комментарии
- Текстовые отзывы видны всем пользователям
- Возможность ответить на отзыв
- Система полезности отзывов (helpful/not helpful)

### 4. Статистика
- Общий рейтинг (средняя оценка)
- Средние оценки по каждому критерию
- Топ-3 полученных комплиментов
- Количество отзывов

## 🔌 API Endpoints

### Создание отзыва
```http
POST /api/reviews/create
Content-Type: application/json

{
  "order_id": 123,
  "reviewer_telegram_id": 123456789,
  "reviewee_telegram_id": 987654321,
  "rating": 5,
  "punctuality_rating": 5,
  "quality_rating": 5,
  "professionalism_rating": 5,
  "communication_rating": 4,
  "vehicle_condition_rating": 5,
  "badges": ["super_driver", "fast_delivery", "punctual"],
  "comment": "Отличный водитель! Все было на высшем уровне.",
  "is_public": true
}
```

### Получение отзывов пользователя
```http
GET /api/reviews/user/{telegram_id}

Response:
{
  "user": {
    "telegram_id": 123456789,
    "name": "Иван Иванов",
    "role": "driver"
  },
  "statistics": {
    "total_reviews": 25,
    "average_rating": 4.8,
    "criteria_averages": {
      "punctuality": 4.9,
      "quality": 4.8,
      "professionalism": 4.7,
      "communication": 4.6,
      "vehicle_condition": 4.9
    },
    "top_badges": [
      {"badge": "super_driver", "label": "⭐ Супер водитель", "count": 18},
      {"badge": "punctual", "label": "⏰ Пунктуальный", "count": 15},
      {"badge": "fast_delivery", "label": "⚡ Быстрая доставка", "count": 12}
    ]
  },
  "reviews": [...]
}
```

### Ответить на отзыв
```http
POST /api/reviews/{review_id}/respond
Content-Type: application/json

{
  "telegram_id": 123456789,
  "response_text": "Спасибо за отзыв! Было приятно работать с вами."
}
```

### Отметить отзыв как полезный
```http
POST /api/reviews/{review_id}/helpful
Content-Type: application/json

{
  "telegram_id": 123456789,
  "is_helpful": true
}
```

### Получить список доступных комплиментов
```http
GET /api/reviews/badges

Response:
{
  "badges": [
    {"id": "super_driver", "label": "⭐ Супер водитель"},
    {"id": "fast_delivery", "label": "⚡ Быстрая доставка"},
    ...
  ]
}
```

## 🖥️ Web интерфейс

### Страница создания отзыва
```
/create-review?order_id=123&reviewer_telegram_id=123456789&reviewee_telegram_id=987654321
```

Красивая интерактивная форма с:
- Звездным рейтингом для каждого критерия
- Визуальным выбором комплиментов
- Текстовым полем для комментария
- Переключателем публичности отзыва

## 📊 Структура базы данных

### Таблица `reviews` (расширенная)
```sql
- id (PRIMARY KEY)
- order_id (связь с заказом)
- reviewer_id (кто оставил отзыв)
- reviewee_id (кого оценили)
- rating (общая оценка 1-5)
- punctuality_rating (пунктуальность 1-5)
- quality_rating (качество 1-5)
- professionalism_rating (профессионализм 1-5)
- communication_rating (общение 1-5)
- vehicle_condition_rating (состояние транспорта 1-5)
- badges (JSON массив выбранных комплиментов)
- comment (текст отзыва)
- is_public (публичный/приватный)
- response_text (ответ на отзыв)
- response_at (когда был дан ответ)
- helpful_count (количество "полезно")
- not_helpful_count (количество "бесполезно")
- created_at (дата создания)
```

### Таблица `review_helpfulness`
```sql
- id (PRIMARY KEY)
- review_id (связь с отзывом)
- user_id (кто отметил)
- is_helpful (полезно/бесполезно)
- created_at (дата)
```

## 🚀 Применение миграции

После деплоя на сервер выполнить:

```bash
ssh root@81.200.147.68 "docker exec freighthub-webapp python /app/migrations/apply_detailed_reviews_migration.py"
```

Миграция безопасна - проверяет существующие колонки перед добавлением.

## 💡 Интеграция с Telegram Bot

### Отправка отзыва после выполнения заказа

```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup

# После завершения заказа отправляем кнопку для отзыва
keyboard = [
    [InlineKeyboardButton(
        "⭐ Оставить отзыв", 
        web_app=WebAppInfo(
            url=f"https://your-domain.com/create-review?"
                f"order_id={order_id}&"
                f"reviewer_telegram_id={reviewer_id}&"
                f"reviewee_telegram_id={reviewee_id}"
        )
    )]
]
reply_markup = InlineKeyboardMarkup(keyboard)
await context.bot.send_message(
    chat_id=user_id,
    text="🎉 Заказ выполнен! Пожалуйста, оцените работу",
    reply_markup=reply_markup
)
```

### Показ рейтинга при выборе водителя

```python
# Получаем статистику водителя
response = requests.get(f"https://your-domain.com/api/reviews/user/{driver_telegram_id}")
stats = response.json()

# Форматируем сообщение
rating = stats['statistics']['average_rating']
reviews_count = stats['statistics']['total_reviews']
top_badges = stats['statistics']['top_badges'][:3]

message = f"""
🚛 *Водитель: {driver_name}*

⭐ Рейтинг: {rating}/5.0 ({reviews_count} отзывов)

🏆 Комплименты:
"""

for badge in top_badges:
    message += f"{badge['label']} ({badge['count']})\n"

await context.bot.send_message(chat_id=user_id, text=message, parse_mode='Markdown')
```

## 📈 Преимущества системы

1. **Прозрачность** - детальная оценка по критериям
2. **Мотивация** - водители стремятся получить хорошие оценки и комплименты
3. **Доверие** - заказчики видят реальные отзывы от других пользователей
4. **Улучшение сервиса** - понятно что именно нужно улучшать
5. **Геймификация** - система комплиментов делает процесс интересным

## 🔮 Планы развития

- [ ] Модерация отзывов (проверка на спам/оскорбления)
- [ ] Уведомления о новых отзывах
- [ ] Возможность прикреплять фото к отзыву
- [ ] Система репутации на основе отзывов
- [ ] Премиум статусы за высокий рейтинг
- [ ] Аналитика по отзывам для водителей

## 🆘 Troubleshooting

### Отзыв не создается
1. Проверьте что заказ существует и имеет статус "completed"
2. Проверьте что пользователь еще не оставлял отзыв для этого заказа
3. Проверьте обязательное поле "rating"

### Миграция не применяется
1. Убедитесь что контейнер запущен: `docker ps`
2. Проверьте путь к базе данных в `truck_config.py`
3. Проверьте права доступа к файлу БД

### Комплименты не загружаются
1. Проверьте что API endpoint `/api/reviews/badges` доступен
2. Проверьте консоль браузера на наличие ошибок
3. Проверьте CORS настройки

## 📞 Поддержка

При возникновении проблем или вопросов обращайтесь к документации API или создавайте issue в репозитории.
